{
  "name": "nka-bulletin-backend",
  "version": "1.0.0",
  "description": "Backend réel pour Nka Bulletin : synchronisation IMAP des bulletins de paie, extraction PDF, notifications push, fusion/export PDF.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "generate-vapid": "web-push generate-vapid-keys",
    "pretest": "node --check server.js && node --check worker.js && node --check src/db.js && node --check src/syncService.js && node --check src/pdfService.js && node --check src/routes/auth.js && node --check src/routes/bulletins.js",
    "predeploy": "npm run pretest"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.1",
    "helmet": "^8.3.0",
    "imapflow": "^1.0.161",
    "jsonwebtoken": "^9.0.2",
    "mailparser": "^3.7.1",
    "morgan": "^1.11.0",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "pdf-lib": "^1.17.1",
    "pdf-parse": "^1.1.1",
    "sql.js": "^1.12.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
